# pyVideoTrans RTX 5080 部署说明

## 安装

1. 将整个 `pyVideoTrans-RTX5080-*` 文件夹放到远程电脑的 `D:\AI\`。
2. 双击 `安装RTX5080环境.cmd`。
3. 安装结束显示“部署完成”后，双击桌面的 `pyVideoTrans RTX5080` 快捷方式。

首次安装需要联网下载 Python 与项目依赖，模型和 FFmpeg 已包含在部署包内。不要把部署目录放到含中文或空格过多的深层路径中；推荐路径为 `D:\AI\pyVideoTrans-RTX5080-*`。

## 运行方式

- 日常启动：双击 `启动pyVideoTrans.cmd` 或桌面快捷方式。
- 字幕消除默认使用自动模式。RTX 5080 显存充足时使用 ProPainter，运行异常才回退 STTN。
- FFmpeg 会优先使用部署包内支持 NVENC 的版本；程序仍以原始质量参数为准，不会通过降低分辨率或码率换速度。

## 故障反馈

安装失败时，将 `deploy-logs` 目录内最新的 `.log` 文件发给 Codex。日志不包含项目 API 密钥。

